const Header = (data) => {
    return (
        <header className="lg:hidden bg-opacity-50 backdrop-filter backdrop-blur-md  bg-white opacity-3 border-b-2 opacity-1 w-screen p-4 border-slate-200 flex items-center justify-center sticky top-0 z-50">
            <p className="texl-2xl font-semibold text-gray-800 text-center">Vedha</p>
        </header>
    )
}

export default Header
